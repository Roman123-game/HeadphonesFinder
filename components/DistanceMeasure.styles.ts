import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  text: {
    position: "absolute",
    top: 20,
    left: 20,

    fontSize: 20,
    fontWeight: "bold",

    color: "#000",
  },

});

export default styles;