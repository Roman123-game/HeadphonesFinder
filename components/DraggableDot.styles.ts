import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  dot: {
    position: "absolute",
    width: 28,
    height: 28,
    borderRadius: 14,
    backgroundColor: "red",
    marginLeft: -14,
    marginTop: -14,
    zIndex: 100,
  },
});

export default styles;