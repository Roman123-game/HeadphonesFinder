import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  dot: {
    position: "absolute",

    width: 40,
    height: 40,

    borderRadius: 20,

    backgroundColor: "red",

    left: 150,
    top: 150,
  },
});

export default styles;